# install-and-configure-standalone-docker-registry
This repo is about installing and configuring a docker registry. It supports both a containerized registry and a rpm based (docker-distribution) registry.
